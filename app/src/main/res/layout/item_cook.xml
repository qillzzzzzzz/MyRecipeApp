<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="8dp"
    android:background="@drawable/bg_input_cookie"
    android:padding="8dp">

    <!-- Header -->
    <Button
        android:id="@+id/btnHeader"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/bg_button_rounded"
        android:backgroundTint="@null"
        android:textColor="@color/cooki_brown"
        android:drawableEnd="@drawable/ic_arrow_down"
        android:padding="12dp"
        android:text="Judul Section" />

    <!-- Isi konten (TextView, bukan EditText) -->
    <LinearLayout
        android:id="@+id/layoutContent"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="8dp"
        android:visibility="gone"
        android:background="@drawable/btn_brown_dropdown_active">

        <TextView
            android:id="@+id/tvIsi"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:minHeight="64dp"
            android:gravity="top"
            android:textColor="@color/coksu"
            android:textSize="16sp"
            android:fontFamily="@font/vanilla_caramel"
            android:padding="8dp"
            android:text="Teks isi di sini..." />
    </LinearLayout>
</LinearLayout>
